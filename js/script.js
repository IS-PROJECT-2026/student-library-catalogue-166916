document.addEventListener("DOMContentLoaded", () => {
    const bookList = document.getElementById("book-list");

    bookList.innerHTML = `
        <p>The library catalogue is ready for development.</p>
    `;
});